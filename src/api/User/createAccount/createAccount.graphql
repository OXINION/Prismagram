type Mutation {
    createAccount(
        username:String!
        email:String!
        firstName:String
        lastName:String
        bii:String
    ): User!
}

type Query{
    something: String!
}